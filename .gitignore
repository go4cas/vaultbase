# Dependencies
node_modules/
webadmin/node_modules/

# Build output
dist/
webadmin/dist/
drizzle/
*.tsbuildinfo

# Compiled binaries (build outputs `cogworks`; keep the old name too for stale local builds)
cogworks
cogworks.exe
vaultbase
vaultbase.exe
releases/*
!releases/.gitignore

# Bun build artifacts
.*.bun-build
index.js.map

# Data dirs (local DB, uploads, logs, secrets) — current + legacy names
cogworks_data/
cogworks_data_test/
vaultbase_data/
vaultbase_data_test/
*.db
*.db-shm
*.db-wal
.secret

# Env / secrets
.env
.env.local
.env.*.local

# Worktrees / IDE
.worktrees/
.claude/
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Internal scratch: roadmap, BUILD_TODO, threat models, future-features, perf
# baselines, superpowers plans/specs. All reside under the repo-root docs/ and
# are NOT for public consumption. Anchored to root (leading slash) so it does
# NOT swallow the Starlight docs content dir at site/src/content/docs/.
/docs/

# Internal security-audit working files (the published advisory lives in
# SECURITY.md; these are pre-publication notes only).
SECURITY-AUDIT-*.md

# Local screenshots / scratch
images-claude/

# Shannon (Keygraph AI pentester) — local-only config + scan deliverables.
# Config holds plaintext throwaway-dev credentials; never commit.
.shannon/
shannon.config.yaml
shannon.yaml
shannon.yml
