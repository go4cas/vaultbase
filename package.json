{
  "name": "cogworks",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "dev:admin": "cd webadmin && bun run dev",
    "dev:docs": "cd site && bun run dev",
    "dev:landing": "cd landing && bun run dev",
    "build:admin": "cd webadmin && bun install --frozen-lockfile && bun run build",
    "build:docs": "cd site && bun run build",
    "build:landing": "cd landing && bun run build",
    "patch:deps": "bun scripts/patch-imagescript.ts",
    "build": "bun run patch:deps && bun run build:admin && bun build --compile --minify --sourcemap=none src/index.ts --outfile cogworks",
    "build:compress": "bun run build && upx --best --lzma cogworks",
    "build:all": "bun run patch:deps && bun run build:admin && bun run build:linux-x64 && bun run build:linux-arm64 && bun run build:linux-x64-musl && bun run build:macos-x64 && bun run build:macos-arm64 && bun run build:windows-x64",
    "build:linux-x64": "bun run patch:deps && bun build --compile --target=bun-linux-x64       --minify --sourcemap=none src/index.ts --outfile releases/cogworks-linux-x64",
    "build:linux-arm64": "bun run patch:deps && bun build --compile --target=bun-linux-arm64     --minify --sourcemap=none src/index.ts --outfile releases/cogworks-linux-arm64",
    "build:linux-x64-musl": "bun run patch:deps && bun build --compile --target=bun-linux-x64-musl  --minify --sourcemap=none src/index.ts --outfile releases/cogworks-linux-x64-musl",
    "build:macos-x64": "bun run patch:deps && bun build --compile --target=bun-darwin-x64      --minify --sourcemap=none src/index.ts --outfile releases/cogworks-macos-x64",
    "build:macos-arm64": "bun run patch:deps && bun build --compile --target=bun-darwin-arm64    --minify --sourcemap=none src/index.ts --outfile releases/cogworks-macos-arm64",
    "build:windows-x64": "bun run patch:deps && bun build --compile --target=bun-windows-x64     --minify --sourcemap=none src/index.ts --outfile releases/cogworks-windows-x64.exe",
    "start": "bun src/index.ts",
    "start:cluster": "bun src/cluster.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src",
    "format": "biome format --write src",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun src/db/migrate.ts",
    "db:reset": "bun src/scripts/reset.ts",
    "compress": "upx --best --lzma releases/cogworks-linux-arm64"
  },
  "dependencies": {
    "@hono/typebox-validator": "^1.1.0",
    "@jsquash/avif": "^2.1.1",
    "@jsquash/webp": "^1.5.0",
    "@simplewebauthn/server": "^13.3.2",
    "@sinclair/typebox": "^0.34.49",
    "cron-parser": "^5.5.0",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.27",
    "imagescript": "^1.3.1",
    "jose": "^5.9.0",
    "jsonpath-plus": "^10.4.0",
    "nodemailer": "^9.0.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@types/bun": "^1.3.0",
    "@types/nodemailer": "^7.0.0",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.8.0"
  },
  "overrides": {
    "esbuild": "^0.25.0"
  }
}
